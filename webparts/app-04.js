window.__APP_SOURCE_PARTS=window.__APP_SOURCE_PARTS||[];window.__APP_SOURCE_PARTS.push("ait api.logout(); state.user = config.demoMode !== false ? await api.init() : null; go('login') })\n  }\n\n  function showLoading(label = 'جارٍ تحميل البيانات...') {\n    app.innerHTML = `<div class=\"loading-state\"><span class=\"spinner\"></span><span>${esc(label)}</span></div>`\n  }\n\n  function showError(message) {\n    app.innerHTML = shell(`<div class=\"notice danger\">${esc(message)}</div>`, 'خطأ')\n    bindShell()\n  }\n\n  async function renderLogin() {\n    if (state.user) { go(''); return }\n    app.innerHTML = `\n      <main class=\"login-page\">\n        <section class=\"login-panel\">\n          <div class=\"login-brand\"><div class=\"brand-mark large\">${icon('shield')}</div><div><h1>أرشيف التحقيقات القيادية</h1><p>نظام داخلي لسيرفر FiveM Roleplay</p></div></div>\n          <div class=\"login-copy\"><span class=\"eyebrow\">دخول مصرح فقط</span><h2>إدارة الملفات والقضايا من مكان واحد</h2><p>ابحث عن أي شخصية، راجع قضاياها، وارفع الصور والفيديوهات والتقارير ضمن سجل منظم.</p></div>\n          <form class=\"form-card login-form\" id=\"loginForm\">\n            ${config.demoMode !== false ? `<div class=\"notice info\"><strong>النسخة التجريبية مفعلة</strong><span>البيانات تحفظ داخل هذا المتصفح فقط.</span></div>` : `\n              <label class=\"field\"><span>البريد الإلكتروني</span><input type=\"email\" name=\"email\" required></label>\n              <label class=\"field\"><span>كلمة المرور</span><input type=\"password\" name=\"password\" required></label>`}\n            <div id=\"loginError\"></div>\n            <button class=\"button button-primary button-full\" type=\"submit\">${icon('shield')}${config.demoMode !== false ? 'دخول النسخة التجريبية' : 'تسجيل الدخول'}</button>\n          </form>\n        </section>\n        <aside class=\"login-visual\"><div class=\"visual-grid\"></div><div class=\"visual-card\">${icon('shield')}<strong>Roleplay Records</strong><span>ليست سجلات أمنية أو قانونية حقيقية</span></div></aside>\n      </main>`\n    document.getElementById('loginForm').addEventListener('submit', async (event) => {\n      event.preventDefault()\n      const button = event.currentTarget.querySelector('button[type=submit]')\n      button.disabled = true\n      try {\n        const form = new FormData(event.currentTarget)\n        state.user = await api.login(form.get('email') || '', form.get('password') || '')\n        go('')\n      } catch (error) {\n        document.getElementById('loginError').innerHTML = `<div class=\"notice danger\">${esc(error.message)}</div>`\n      } finally { button.disabled = false }\n    })\n  }\n\n  async function renderDashboard() {\n    showLoading()\n    const [people, cases] = await Promise.all([api.listPeople(), api.listCases()])\n    const stats = {\n      people: people.length,\n      open: cases.filter((item) => item.status === 'open').length,\n      review: cases.filter((item) => item.status === 'review').length,\n      closed: cases.filter((item) => item.status === 'closed').length,\n    }\n    const recent = cases.slice(0, 5)\n    const rows = recent.map((item) => {\n      const person = people.find((candidate) => candidate.id === item.person_id)\n      return `<tr class=\"clickable-row\" data-case=\"${esc(item.id)}\"><td class=\"mono\">${esc(item.case_number)}</td><td><strong>${esc(item.title)}</strong></td><td>${esc(person?.full_name || '—')}</td><td>${badge(item.status)}</td><td>${badge(item.priority)}</td><td>${formatDate(item.updated_at)}</td></tr>`\n    }).join('')\n\n    const content = `\n      <div class=\"page-stack\">\n        <section class=\"hero-card\">\n          <div><span class=\"eyebrow light\">بحث سريع</span><h2>ابحث باسم الشخصية أو رقمها العسكري</h2><p>ستظهر لك جميع القضايا والتقارير والمرفقات المرتبطة بالملف.</p></div>\n          <form class=\"hero-search\" id=\"dashboardSearch\">${icon('search')}<input name=\"query\" placeholder=\"مثال: خالد العلي أو 1001\"><button class=\"button button-light\">بحث</button></form>\n        </section>\n        <section class=\"stats-grid\">\n          <article class=\"stat-card\"><div class=\"stat-icon\">${icon('people')}</div><div><span>ملفات الأشخاص</span><strong>${stats.people}</strong></div></article>\n          <article class=\"stat-card\"><div class=\"stat-icon warning\">${icon('folder')}</div><div><span>قضايا مفتوحة</span><strong>${stats.open}</strong></div></article>\n          <article class=\"stat-card\"><div class=\"stat-icon review\">${icon('clock')}</div><div><span>قيد المراجعة</span><strong>${stats.review}</strong></div></article>\n          <article class=\"stat-card\"><div class=\"stat-icon success\">${icon('check')}</div><div><span>قضايا مغلقة</span><strong>${stats.closed}</strong></div></article>\n        </section>\n        <section class=\"content-card\">\n          <div class=\"section-heading\"><div><span class=\"eyebrow\">آخر النشاطات</span><h2>القضايا الأحدث</h2></div><a href=\"#/people\" class=\"text-link\">عرض ملفات الأشخاص</a></div>\n          <div class=\"table-wrap\"><table><thead><tr><th>رقم القضية</th><th>العنوان</th><th>صاحب الملف</th><th>الحالة</th><th>الأولوية</th><th>آخر تحديث</th></tr></thead><tbody>${rows || '<tr><td colspan=\"6\">لا توجد قضايا.</td></tr>'}</tbody></table></div>\n        </section>\n        <div class=\"disclaimer-card\">${icon('alert')}<div><strong>تنبيه استخدام</strong><span>هذا النظام مخصص لسجلات لعب الأدوار داخل FiveM فقط، وليس للاستخدام الأمني أو القانوني الحقيقي.</span></div></div>\n      </div>`\n    app.innerHTML = shell(content, 'لوحة التحكم', 'dashboard')\n    bindShell()\n    document.querySelectorAll('[data-case]').forEach((row) => row.addEventListener('click', () => go(`case/${row.dataset.case}`)))\n    document.getElementById('dashboardSearch').addEventListener('submit', (event) => {\n      event.preventDefault()\n      const query = String(new FormData(event.currentTarget).get('query') || '').trim().toLowerCase()\n      const person = people.find((item) => [item.full_name, item.military_number].some((value) => String(value || '').toLowerCase().includes(query)))\n      person ? go(`person/${person.id}`) : go(`people?q=${encodeURIComponent(query)}`)\n    })\n  }\n\n  async function renderPeople(query = '') {\n    showLoading()\n    const [people, cases] = await Promise.all([api.listPeople(), api.listCases()])\n    const cards = people.map((person) => {\n      const personCases = cases.filter((item) => item.person_id === person.id)\n      const active = personCases.filter((item) => ['open', 'review'].includes(item.status)).length\n      const search = [person.full_name, person.rank, person.military_number, person.department].join(' ').toLowerCase()\n      return `<article class=\"person-card\" data-pers");
